# Supabase (required for auth)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Wasabi (S3-compatible storage) — required for image uploads (inventory, etc.)
# See docs/storage/wasabi.md for setup. Do not expose secret keys to the client.
# WASABI_ACCESS_KEY=
# WASABI_SECRET_KEY=
# WASABI_BUCKET=
# WASABI_REGION=us-east-1
# WASABI_ENDPOINT=https://s3.us-east-1.wasabisys.com
# Optional: base URL for public object links (e.g. CDN). If unset, derived from endpoint + bucket.
# WASABI_PUBLIC_BASE_URL=

# eBay OAuth (production API only). See docs/ebay/oauth.md.
# EBAY_CLIENT_ID=
# EBAY_CLIENT_SECRET=
# EBAY_RUNAME=   (RuName from eBay Developer Portal > User Tokens > Redirect URL name)
# Optional: app base URL for post-callback redirect. Defaults to VERCEL_URL on Vercel.
# NEXT_PUBLIC_APP_URL=
